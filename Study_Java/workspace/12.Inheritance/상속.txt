○ 상속(Inheritance)
	- 정의 : 상위클래스(슈퍼클래스)로부터 하위클래스(서브클래스)가 물려받는다.
	- 어떤 것을 상속하는가? : 상태정보(멤버변수, 필드), 행위정보(메소드)
	- 재정의(Override) : 하위클래스에서 상속받은 메소드를 수정
	- extends : public class A extends B ▶ A(하위) → B(상위)
	- Java는 단일상속만 허용하는 문제점을 해결하기 위해 인터페이스가 등장
	 